답글은 무조건 업데이트 후에 인서트 한다.
내림차순으로 정렬
상대주소는 나를 기준으로 찾으니까 고려할 필요가 없는데, 
절대주소를 쓰면 context명이 들어가니까 경로가 있는 주소가 있는 각 페이지에서 일일히 다 바꿔줘야함.
절대주소를 쓰면 파일구조(depth)가 노출됨. 
반면 상대주소는 나를 기준으로 되니까 파일구조 파악이 쉽진 않아요.
request.hetContextPath() 하니까 Context주소가 찍히네요.

day37/ root보다 한 경로 아래에 있는거고,
day37 = root와 동일선상에 있는거에요.
--------------------------------------------------------------------
dummytext / dummytext한글

Java Resources에 src에 패키지(com.hi.bean) 생성
클래스 생성. 자르 파일 생성하지 않고 만드는 방법

JavaBean
-- 필드 정의하고 getter setter하는것
--  정보를 제어하는.
-- 디폴트는 동일패키지에서만 접근가능합니다. 

getter & setter
getter 은 객체의 attribute(변수) 를 반환하고 setter 은
파라미터를 받아서 attribute 에 할당한다.

크게 달라진 건 없어요.
어제 자르로 했던 파일이 코드가 안으로 들어왔고요,
private시키고 메서드 선언하면서 


함수형 언어의 반복문을 본따서 만든 게 '개선된 루프문'
property는 넘어오는 어떤 파라미터? 를 의미.

getter setter를 만들 때 필드명 만들어내는 규칙 지켜야 함. (대소문자..)
--------------------------------------------------------------------------

scope
 =page : 하나의 jsp페이지 내에서만 객체를 공유하는 영역. 실제 선언된 jsp페이지 내에서만 사용할 수 있음
 =request : 요청을받아서 응답하기까지 객체가 유효한 영역
 =session : 접속 당 하나. 같은 브라우저 내에서 요청되는 페이지들은 같은 객체를 공유하는 영역(하나의 웹 브라우저와 관련된 영역)
 =applicaton : 웹 어플리케이션이 시작되고 종료(혹은 다시 시작)될 때까지 변수를 사용할 수 있음




